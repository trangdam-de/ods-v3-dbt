TRUNCATE TABLE {{ params.des_schema_name }}.{{ params.des_table_name }}; 
INSERT INTO {{ params.des_schema_name }}.{{ params.des_table_name }} 
(
        contract_id,
        contract_name,
        contract_number,
        accnt_id,
        contract_sig_date,
        contract_srv,
        contract_valid_date,
        contract_exp_date,
        contract_renew_date,
        contract_renew_due,
        created_date,
        created_user,
        contract_status,
        contract_mgr_org,
        managed_user,
        contract_address_sign,
        contract_method_pay,
        contract_auto_renew,
        contract_type,
        contract_attached_file,
        contract_type_service,
        contract_expiry_renewal_date,
        contract_sig_date_first,
        payment_limit,
        contract_service_cat,
        contract_classify
    )
    SELECT
        src.contract_id,
        src.contract_name,
        src.contract_number,
        src.accnt_id,
        src.contract_sig_date,
        src.contract_srv,
        src.contract_valid_date,
        src.contract_exp_date,
        src.contract_renew_date,
        src.contract_renew_due,
        src.created_date,
        src.created_user,
        src.contract_status,
        src.contract_mgr_org,
        src.managed_user,
        src.contract_address_sign,
        src.contract_method_pay,
        src.contract_auto_renew,
        src.contract_type,
        src.contract_attached_file,
        src.contract_type_service,
        src.contract_expiry_renewal_date,
        src.contract_sig_date_first,
        src.payment_limit,
        src.contract_service_cat,
        src.contract_classify
    FROM staging.{{ params.des_schema_name }}_{{ params.des_table_name }} src;
